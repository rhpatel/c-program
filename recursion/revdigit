#include<stdio.h>
int sum(int);
main()
{
int tot,num;
printf("Enter the number..\n");
scanf("%d",&num);
tot=sum(num);
printf("total=%d\n",tot);
}

int sum(int n)
{ 
if(n>0)
 {
  int r=n%10;
  return(r+sum(n/10));
 }
else
return;
}
